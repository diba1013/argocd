victoria-metrics:
  vmsingle:
    spec:
      storage:
        storageClassName: local-path
  grafana:
    ingress:
      enabled: true
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-cloudflare
      hosts:
        # These need to be overwritten manually, since templating is not supported in every usage sadly.
        - grafana.localhost
      tls:
        - hosts:
            # These need to be overwritten manually, since templating is not supported in every usage sadly.
            - grafana.localhost
          secretName: grafana-crt
