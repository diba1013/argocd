{{- $permissions := .Values.permissions }}
{{- $postgres := .Values.postgres }}
---
apiVersion: v1
kind: Service
metadata:
  name: database
spec:
  clusterIP: None # Headless service for StatefulSet identity
  selector:
    app: immich-postgres
  ports:
    - protocol: TCP
      port: 5432
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: immich-postgres
spec:
  serviceName: database
  replicas: 1
  selector:
    matchLabels:
      app: immich-postgres
  template:
    metadata:
      labels:
        app: immich-postgres
    spec:
      initContainers:
        - name: fix-permissions
          image: {{ printf "%s:%s" $permissions.image.repository $permissions.image.tag }}
          command:
            - sh
            - -c
            - |
              if [ -z "$(ls /var/lib/postgresql/data)" ]; then
                echo "Data directory is empty, setting permissions..."
                chmod 755 /var/lib/postgresql/data
              else
                echo "Data directory not empty, fixing ownership..."
                chown -R 999:999 /var/lib/postgresql/data
              fi
          securityContext:
            runAsUser: 0
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
      containers:
        - name: postgres
          image: {{ printf "%s:%s" $postgres.image.repository $postgres.image.tag }}
          env:
            - name: POSTGRES_INITDB_ARGS
              value: "--data-checksums"
          envFrom:
            - secretRef:
                name: immich-postgres
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
            - name: dshm
              mountPath: /dev/shm
          # Updated Healthchecks using the internal script
          readinessProbe:
            exec:
              command: ["/usr/local/bin/healthcheck.sh"]
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["/usr/local/bin/healthcheck.sh"]
            initialDelaySeconds: 30
            periodSeconds: 30
      volumes:
        - name: dshm
          emptyDir:
            medium: Memory
            sizeLimit: 128Mi
  volumeClaimTemplates:
    - apiVersion: v1
      kind: PersistentVolumeClaim
      metadata:
        name: postgres-data
      spec:
        storageClassName: local-path
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi
